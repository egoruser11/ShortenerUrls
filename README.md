🔗 URL Shortener Service

Лёгкий и минималистичный сервис для сокращения длинных URL-адресов.
Позволяет создавать короткие псевдонимы (alias) и перенаправлять на исходные ссылки.

🚀 Требования

Go 1.21+

SQLite — создаётся автоматически при первом запуске

▶️ Запуск проекта
# Установка зависимостей
go mod download

# Запуск сервера
go run main.go

🏗 Архитектура

Разделение на слои (чистая архитектура)

Встроенная SQLite база данных

Валидация входных данных

Структурированное логирование

Централизованная обработка ошибок

🗂 Структура базы данных
Таблица url
Поле	Тип	Описание
alias	TEXT	Уникальный идентификатор (PRIMARY KEY)
url	TEXT	Исходный длинный URL
📡 API Endpoints
1. Создание короткой ссылки

POST /url

Тело запроса:
{
"url": "https://example.com/very-long-path",
"alias": "example"
}

Правила:

url — обязательное поле, валидный URL

alias — опционально, если не передан → генерируется автоматически

Если alias уже существует → ошибка "url already exists"

2. Получение списка всех псевдонимов

GET /url/aliases

Пример ответа:
["alias1", "alias2", "alias3"]

3. Редирект по короткой ссылке

GET /{alias}

Поведение:

Если схема отсутствует — автоматически добавляется https://

Выполняется HTTP-редирект на исходный URL

4. Удаление ссылки

POST /delete

Тело запроса:
{
"alias": "example"
}

Правила:

alias — обязательное поле

⭐ Особенности реализации

Генерация alias без коллизий

Автоматическое создание SQLite БД

Подробное логирование операций

Понятная обработка ошибок

